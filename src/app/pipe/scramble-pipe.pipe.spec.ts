import { ScramblePipe } from './scramble-pipe.pipe';

describe('ScramblePipe', () => {
  it('create an instance', () => {
    const pipe = new ScramblePipe();
    expect(pipe).toBeTruthy();
  });
});
